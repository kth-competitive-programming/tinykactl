\problemtitle{Minimum Spanning Tree}

\begin{algorithm}{Prim}
\reflisting{prim}
\complexity{V\log V + E}
\usage{\sourceline{
prim( graph, path, 0 ) }}
\end{algorithm}

\begin{algorithm}{Kruskal}
\usage{ \sourceline{kruskal( graph, tree, n );} }
\complexity{ E\log E }
\characteristics{ The resulting tree which is returned in {\tt tree} may
be the same variable as the graph. }
\begin{example}
	vector< vector<pair<int,double> > > edges;

	edges.resize( 100 );
	kruskal( edges, edges, 100 );
\end{example}
\reflisting{sets}
\reflisting{kruskal}
\valladolid{10147}
\end{algorithm}
