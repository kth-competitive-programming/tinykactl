\problemtitle{Minimum Spanning Tree}

\begin{algorithm}{Prim}
\reflisting{dijkstra prim}
\complexity{ E\log V }
\usage{\sourceline{%
dijkstra\_prim( 0, graph, minarray, path, distfun, inf, true ) }}
\end{algorithm}

\begin{algorithm}{Kruskal}
\usage{ \sourceline{kruskal( graph, tree, n );} }
\complexity{ E\log E }
\characteristics{Kruskal is faster when dealing with...}
\begin{example}
	vector< vector<pair<int,double> > > edges;

	edges.resize( 100 );
	kruskal( edges, edges, 100 );
\end{example}
\reflisting{kruskal}
\valladolid{10147}
\end{algorithm}
